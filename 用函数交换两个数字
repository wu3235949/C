#include <stdio.h>
void swap(int* pa,int* pb);
int main() {
	int a,b;
	scanf("%d%d",&a,&b);
	printf("a=%d,b=%d\n",a,b);
	swap(&a,&b);
	printf("a=%d,b=%d\n",a,b);
}
void swap(int* pa,int* pb) {
	int mid;
	mid = *pa;
	*pa=*pb;
	*pb=mid;
}
